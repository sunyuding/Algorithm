/**
 * For this problem,
 * you will write a program that prints the Nth largest value in a fixed sized array of integers.
 * To make things simple,
 * N will be 3 and the array will always be have 10 decimal integer values.
 *
 *
 *
 * Input: 10 decimal integers whose values are between 1 and 1000 inclusive
 *
 *
 * Output: the 3rd largest value of the corresponding 10 integers.
 *
 * Easy
 * Array
 */
public class NthLargestValue {
    public int getThirdLargest(int[] array) {
        return 0;
    }
}
